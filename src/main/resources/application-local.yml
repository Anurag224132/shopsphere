# =====================================================
# LOCAL Development Profile
# Activated by: spring.profiles.active=local
# =====================================================

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    shopsphere.backend: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
