# =====================================================
# STAGING Profile
# Activated by: spring.profiles.active=staging
# =====================================================

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    shopsphere.backend: INFO
    org.springframework.security: INFO
