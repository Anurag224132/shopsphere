# =====================================================
# STAGING Profile
# Activated by: spring.profiles.active=staging
# =====================================================

spring:
  jpa:
    hibernate:
      ddl-auto: validate     # validates schema matches entities — no structural changes
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    shopsphere.backend: INFO
    org.springframework.security: INFO
